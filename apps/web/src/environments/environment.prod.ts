export const environment = {
  production: true,
  /** REST vive na raiz da API (o Nest não usa globalPrefix); Better Auth em /api/auth. */
  apiUrl: '',
  authUrl: '/api/auth',
  vapidPublicKey: '',
};
