export const environment = {
  production: false,
  apiUrl: 'http://localhost:3000',
  authUrl: 'http://localhost:3000/api/auth',
  /** Chave pública VAPID (é pública por definição). Vazia = push desligado na UI. */
  vapidPublicKey: '',
};
